services:
  - type: web
    name: mot
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: XAI_API_KEY
        sync: false
